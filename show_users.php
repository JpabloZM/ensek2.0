<?php require 'vendor/autoload.php';  = require_once 'bootstrap/app.php'; ->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); echo "Usuarios con sus roles:\n";  = DB::table('users')->join('roles', 'users.role_id', '=', 'roles.id')->select('users.id', 'users.name', 'users.email', 'roles.name as role_name')->get(); foreach( as ) { echo "{->id}. {->name} ({->email}) - {->role_name}\n"; }
